% Answer to Problem 1. This file is \input into the parent document,
% so it carries no preamble and no \section -- the heading lives in
% the parent so the table of contents stays in one place.

Replace this paragraph with your answer.

\begin{description}
  \item[A description list styles its label in Heritage Blue.]
    Long labels wrap onto the next line instead of running past the
    right margin, and the body text starts on its own line.
\end{description}
